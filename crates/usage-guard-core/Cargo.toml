[package]
name = "usage-guard-core"
version = "1.0.0-alpha.1"
edition = "2021"
license = "MIT"
description = "Deterministic core policy engine for Codex Usage Guard"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
